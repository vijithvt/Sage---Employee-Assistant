"""Reserved for future retrieval logic (e.g. re-ranking, hybrid search).

Not currently used — retrieval today happens directly via
`vectorstore.similarity_search()`, called from `rag_pipeline.answer_query()`.
This module is a placeholder seam for V0.2 "RAG Engineering" work
(see docs/course-roadmap.md); nothing imports it yet.
"""
